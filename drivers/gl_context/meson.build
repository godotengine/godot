if PLATFORM == 'windows' or PLATFORM == 'macos' or PLATFORM == 'linuxbsd'
    SOURCES += files('../../thirdparty/glad/gl.c')

    CPP_ARGS += [
        '-DGLAD_ENABLED',
        '-DEGL_ENABLED',
    ]
endif
