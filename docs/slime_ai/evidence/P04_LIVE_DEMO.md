# P04 live-provider verification — not_run

No paid OpenAI request was started during this assignment. The user did not select an explicit model and authorize a finite live run with selected-scene context. `live_provider` therefore remains **not_run**, even though the OpenAI Responses adapter compiles and 35 offline service cases pass. No provider response, live token usage, model quality, real account access, or live network retry behavior is claimed.

The editor defaults to OpenAI Responses as the one real provider option and also offers an explicitly labeled offline fake. A live run requires an exact model string and one-run authorization in the dock. The native controller fixes limits to 3 attempts, 4 tool calls, 1024 output tokens per request, 30 seconds per request, and 120 seconds overall, and exposes the selected-scene context manifest before dispatch. The service reads the user-scoped Windows Credential Manager target `SlimeEngine/OpenAI`; missing credentials yield a local credential-status/error result. The narrow development fallback requires `SLIME_AI_DEV_CREDENTIAL_FALLBACK=1`. No key is stored in the repo, journal, fixture, command line, or evidence.

Offline tests inject HTTP and SSE fixtures for `response.completed`, failure, incomplete output, split Unicode, strict tool schemas, call ID continuation, cancellation, 401/403, 429, timeouts, connection loss, and redaction. The host dispatches no tool from an incomplete/failed/cancelled/truncated turn, and native preview/grant/revision/apply/status remains authoritative. These tests are evidence of local logic, not of live service behavior.

The next gate is a single authorized live run on a copied trusted fixture with a chosen model and reviewed context. Record the configured model and limits, consent scope, scrubbed event trace, actual attempt/tool counts and provider usage when available, the native operation ID and revision, editor save result, scene hash, and reopen result. If the live provider reports no usage or pricing basis, display it as unknown; do not call it free or claim a monetary cap. Stop at the P04 handoff until that authorization exists.
