#!/usr/bin/env python
from misc.utility.scons_hints import *

import sys

Import("env", "py_builder_env")

env.add_source_files(env.scene_sources, "*.cpp")

SConscript("icons/SCsub")

py_builder_env.Command(
    "#scene/theme/default_font.gen.h",
    "#thirdparty/fonts/OpenSans_SemiBold.woff2",
    Action(
        "$PYTHON_BIN scene/theme/default_theme_builders.py --method make_fonts_header --target $TARGET --source $SOURCE",
        "$GENCOMSTR",
    ),
)
