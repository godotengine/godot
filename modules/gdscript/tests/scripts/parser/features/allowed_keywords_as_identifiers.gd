func test():
	# The following keywords are allowed as identifiers:
	var match = "match"
	print(match)

	var PI = "PI"
	print(PI)

	var INF = "INF"
	print(INF)

	var NAN = "NAN"
	print(NAN)

	var TAU = "TAU"
	print(TAU)

	# New keyword for pattern guards.
	var when = "when"
	print(when)
