<?xml version="1.0" encoding="UTF-8" ?>
<class name="AudioStreamMP3" inherits="AudioStream" version="4.0">
	<brief_description>
		MP3 audio stream driver.
	</brief_description>
	<description>
		MP3 audio stream driver. See [member data] if you want to load an MP3 file at run-time.
	</description>
	<tutorials>
	</tutorials>
	<methods>
	</methods>
	<members>
		<member name="data" type="PackedByteArray" setter="set_data" getter="get_data" default="PackedByteArray()">
			Contains the audio data in bytes.
			You can load a file without import using the method below. Keep in mind that this method loads the whole file into memory and may be not the prefered way for big files.
			[codeblocks]
			[gdscript]
			func load_ogg(path):
			    var file = File.new()
			    file.open(path, File.READ)
			    var sound = AudioStreamMP3.new()
			    sound.data = file.get_buffer(file.get_len())
			    file.close()
			    return sound
			[/gdscript]
			[csharp]
			public AudioStreamMP3 LoadOgg(string path)
			{
			    var file = new File();
			    file.Open(path, File.READ);
			    var sound = new AudioStreamMP3();
			    sound.Data = file.GetBuffer(file.GetLen());
			    file.Close();
			    return sound;
			}
			[/csharp]
			[/codeblocks]
		</member>
		<member name="loop" type="bool" setter="set_loop" getter="has_loop" default="false">
			If [code]true[/code], the stream will automatically loop when it reaches the end.
		</member>
		<member name="loop_offset" type="float" setter="set_loop_offset" getter="get_loop_offset" default="0.0">
			Time in seconds at which the stream starts after being looped.
		</member>
	</members>
	<constants>
	</constants>
</class>
