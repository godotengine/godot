# ruff: noqa: F821
Import("env")

# Only compiles if it is web
if env["platform"] == "web":
    env.add_source_files(
        env.modules_sources,
        [
            "register_types.cpp",
            "tests/C++/tests_caller.cpp",
            "command_processor.cpp",
        ],
    )
