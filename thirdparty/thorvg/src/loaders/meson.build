thorvg_sources += files(run_command(GLOB, 'raw/*.cpp', check: true).stdout().split('\n'))
thorvg_sources += files(run_command(GLOB, 'png/*.cpp', check: true).stdout().split('\n'))
thorvg_sources += files(run_command(GLOB, 'jpg/*.cpp', check: true).stdout().split('\n'))
thorvg_sources += files(run_command(GLOB, 'svg/*.cpp', check: true).stdout().split('\n'))
thorvg_sources += files(run_command(GLOB, 'tvg/*.cpp', check: true).stdout().split('\n'))
