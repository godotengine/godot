# Projection validation artifacts

_Last updated: 2025-11-27_

This directory is intentionally kept in the repository as a destination for GPU/CPU comparison artifacts generated by the projection validation tests. The tests write files such as `projection_validation.png` here at runtime, but no binary outputs are tracked in git.
