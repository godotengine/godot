
extends RigidBody2D

# member variables here, example:
# var a=2
# var b="textvar"

func _ready():
	# Initialization here
	set_process_input(true)

	
#func _input(event):
	#if(event.type == InputEvent.MOUSE_MOTION):
	#	var pos = event.pos
	#	if(pos.y > 50):
	#		self.set_pos(pos-self.get_parent().get_parent().get_parent().get_pos())
			#-traveler.get_parent().get_pos()
